kumuluzee:
  name: reservations-service
  env:
    name: dev
  version: 1.0.0
  server:
    base-url: http://localhost:8080
    http:
      port: 8080
  datasources:
    - jndi-name: jdbc/ReservationsDS
      connection-url: jdbc:postgresql://pg-reservations:5432/reservations
      username: dbuser
      password: postgres
  config:
    consul:
      agent: http://consul:8500
